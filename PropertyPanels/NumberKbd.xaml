<UserControl x:Class="SglDesigner.PropertyPanels.NumberKbdPropertyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:base="clr-namespace:SglDesigner.PropertyPanels" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SglDesigner.PropertyPanels">

    
    <StackPanel Margin="5">
        <base:BasePropertyPanel />
        
       

     

        <TextBlock Text="按键与文本设置" Style="{StaticResource PropHeader}"/>
        <UniformGrid Columns="2">
            <StackPanel Margin="2">
                <TextBlock Text="按键颜色" Foreground="Gray" FontSize="11"/>
                <xctk:ColorPicker SelectedColor="{Binding BtnColor}" Height="24"/>
            </StackPanel>
            <StackPanel Margin="2">
                <TextBlock Text="文本颜色" Foreground="Gray" FontSize="11"/>
                <xctk:ColorPicker SelectedColor="{Binding TextColor}" Height="24"/>
            </StackPanel>
            <StackPanel Margin="2">
                <TextBlock Text="按键圆角" Foreground="Gray" FontSize="11"/>
                <xctk:IntegerUpDown Value="{Binding BtnRadius}" Height="24" Minimum="0" Maximum="255"/>
            </StackPanel>
            <StackPanel Margin="2">
                <TextBlock Text="按键边框宽" Foreground="Gray" FontSize="11"/>
                <xctk:IntegerUpDown Value="{Binding BtnBorderWidth}" Height="24" Minimum="0" Maximum="10"/>
            </StackPanel>
            <StackPanel Margin="2">
                <TextBlock Text="按键边距" Foreground="Gray" FontSize="11"/>
                <xctk:IntegerUpDown Value="{Binding BtnMargin}" Height="24" Minimum="0" Maximum="10"/>
            </StackPanel>
            <StackPanel Margin="2">
                <TextBlock Text="按键边框颜色" Foreground="Gray" FontSize="11"/>
                <xctk:ColorPicker SelectedColor="{Binding BtnBorderColor}" Height="24"/>
            </StackPanel>
        </UniformGrid>

        <StackPanel Margin="2">
            <TextBlock Text="字体选择" Foreground="Gray"/>
            <ComboBox SelectedValue="{Binding FontName}"/>
        </StackPanel>
        <StackPanel Margin="2">
            <TextBlock Text="关联文本框 (target)" Foreground="Gray" FontSize="11"/>
            <ComboBox SelectedValue="{Binding TargetTextArea}" Height="24"
              ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.AvailableTextAreas}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
