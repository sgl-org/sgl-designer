<UserControl x:Class="SglDesigner.PropertyPanels.StatusbarPropertyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:SglDesigner"
             Width="236">
    <StackPanel>
        <TextBlock Text="外观" Style="{StaticResource PropHeader}"/>
        <UniformGrid Columns="2">
            <StackPanel Margin="2">
                <TextBlock Text="背景颜色" Foreground="Gray" />
                <xctk:ColorPicker SelectedColor="{Binding BgColor}"/>
            </StackPanel>
            <StackPanel Margin="2">
                <TextBlock Text="背景透明度" Foreground="Gray" />
                <xctk:IntegerUpDown Value="{Binding BgAlpha}" Minimum="0" Maximum="255"/>
            </StackPanel>
            <StackPanel Margin="2">
                <TextBlock Text="圆角" Foreground="Gray" />
                <xctk:IntegerUpDown Value="{Binding Radius}"/>
            </StackPanel>
            <StackPanel Margin="2">
                <TextBlock Text="字体选择 (font)" Foreground="Gray" />
                <ComboBox SelectedValue="{Binding FontName}"
                    ItemsSource="{Binding Source={x:Static local:FontViewModel.Instance}, Path=FontOptions}"/>
            </StackPanel>
        </UniformGrid>

        <TextBlock Text="槽位布局" Style="{StaticResource PropHeader}"/>
        <UniformGrid Columns="2">
            <StackPanel Margin="2">
                <TextBlock Text="左边距" Foreground="Gray" />
                <xctk:IntegerUpDown Value="{Binding LeftMargin}" Minimum="0" Maximum="100"/>
            </StackPanel>
            <StackPanel Margin="2">
                <TextBlock Text="右边距" Foreground="Gray" />
                <xctk:IntegerUpDown Value="{Binding RightMargin}" Minimum="0" Maximum="100"/>
            </StackPanel>
            <StackPanel Margin="2">
                <TextBlock Text="槽位间距" Foreground="Gray" />
                <xctk:IntegerUpDown Value="{Binding SlotSpace}" Minimum="0" Maximum="50"/>
            </StackPanel>
        </UniformGrid>
    </StackPanel>
</UserControl>
