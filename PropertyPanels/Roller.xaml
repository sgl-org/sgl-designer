<UserControl x:Class="SglDesigner.PropertyPanels.RollerPropertyPanel"
             xmlns:local="clr-namespace:SglDesigner"
             xmlns:panels="clr-namespace:SglDesigner.PropertyPanels"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" >
    <StackPanel>
        <panels:BasePropertyPanel />

        <TextBlock Text="滚动选项设置" Style="{StaticResource PropHeader}"/>
        <Border BorderBrush="#3F3F3F" BorderThickness="1" Margin="2" Background="#2D2D2D">
            <StackPanel Margin="5">
                <TextBlock Text="选项文本 (每行一个选项)" Foreground="Gray" Margin="0,0,0,3"/>
                <TextBox Text="{Binding Options, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" Height="100" Margin="0,0,0,5"
                         Background="#1E1E1E" Foreground="White"
                         VerticalScrollBarVisibility="Auto"/>

                <StackPanel Margin="0,5">
                    <TextBlock Text="可见行数 (visible_rows)" Foreground="Gray" />
                    <xctk:IntegerUpDown Value="{Binding VisibleRows}" Minimum="1" Maximum="7"/>
                </StackPanel>

                <StackPanel Margin="0,5">
                    <TextBlock Text="默认选中项索引 (0 开始)" Foreground="Gray" />
                    <xctk:IntegerUpDown Value="{Binding SelectedIndex}" Minimum="0" Maximum="99"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <TextBlock Text="文本样式" Style="{StaticResource PropHeader}"/>

        <UniformGrid Columns="2">
            <StackPanel Margin="2">
                <TextBlock Text="文字颜色 (text_color)" Foreground="Gray" Margin="0 2 0 2"/>
                <xctk:ColorPicker SelectedColor="{Binding TextColor}"/>
            </StackPanel>

            <StackPanel Margin="2">
                <TextBlock Text="选中颜色 (selected_color)" Foreground="Gray" Margin="0 2 0 2"/>
                <xctk:ColorPicker SelectedColor="{Binding SelectedColor}"/>
            </StackPanel>

            <StackPanel Margin="2">
                <TextBlock Text="字体选择 (font)" Foreground="Gray" Margin="0 2 0 2"/>
                <ComboBox SelectedValue="{Binding FontName}"
                  ItemsSource="{Binding Source={x:Static local:FontViewModel.Instance}, Path=FontOptions}"/>
            </StackPanel>
        </UniformGrid>

        <TextBlock Text="外观样式" Style="{StaticResource PropHeader}"/>
        <UniformGrid Columns="2">
            <StackPanel Margin="2">
                <TextBlock Text="背景颜色 (bg_color)" Foreground="Gray" />
                <xctk:ColorPicker SelectedColor="{Binding BgColor}"/>
            </StackPanel>

            <StackPanel Margin="2">
                <TextBlock Text="边框颜色 (border_color)" Foreground="Gray" />
                <xctk:ColorPicker SelectedColor="{Binding BorderColor}"/>
            </StackPanel>

            <StackPanel Margin="2">
                <TextBlock Text="透明度 (alpha)" Foreground="Gray" />
                <xctk:IntegerUpDown Value="{Binding Alpha}" Minimum="0" Maximum="255"/>
            </StackPanel>

            <StackPanel Margin="2">
                <TextBlock Text="圆角 (radius)" Foreground="Gray" />
                <xctk:IntegerUpDown Value="{Binding Radius}" Minimum="0" Maximum="50"/>
            </StackPanel>

            <StackPanel Margin="2">
                <TextBlock Text="边框宽度 (border_width)" Foreground="Gray" />
                <xctk:IntegerUpDown Value="{Binding BorderWidth}" Minimum="0" Maximum="10"/>
            </StackPanel>
        </UniformGrid>

    </StackPanel>
</UserControl>
